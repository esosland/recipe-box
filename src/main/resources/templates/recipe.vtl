<h2>$recipe.getName()</h2>

<h3>Tags for this recipe:</h3>
<ul>
  #foreach($tag in $allTags)
    <li>$tag.getType()</li>
  #end
</ul>


<form class="well" action="index.html" method="post">
  <h2>this is the drop-down for adding a new tag</h2>
  <button type="button" name="button">this button does nothing right now.</button>
</form>

<h3>Ingredients:</h3>
<p>$recipe.getIngredients()</p>

<h3>Directions:</h3>
<p>$recipe.getDirections()</p>
